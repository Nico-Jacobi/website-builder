/**
 * Re-export of the shared element schemas (see @website-builder/shared).
 * Kept as a barrel so module files can continue importing from this path.
 */
export { LinkSchema, CardSchema } from '@website-builder/shared';
export type { Link, CardData } from '@website-builder/shared';
