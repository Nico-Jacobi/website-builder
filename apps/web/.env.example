# Optional: überschreibt den Default http://localhost:3001
# (siehe siteClient.ts und llm/generateSpec.ts / llm/refineSpec.ts).
#
# LLM- und Pixabay-Keys laufen jetzt ausschließlich im Backend — siehe
# apps/api/.env bzw. apps/api/.env.example.

VITE_API_BASE=http://localhost:3001
