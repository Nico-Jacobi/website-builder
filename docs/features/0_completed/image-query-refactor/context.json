{
  "feature": "image-query-refactor",
  "masterplan_path": "docs/features/image-query-refactor/Masterplan.md",

  "files": {
    "created": [],
    "deleted": [],
    "modified": []
  },

  "new_exports": {
    "services": [],
    "components": [],
    "hooks": [],
    "types": [],
    "utils": [],
    "endpoints": []
  },

  "breaking_changes": [
    {
      "what": "fillImages() signature changed",
      "from": "fillImages(spec, userPrompt)",
      "to": "fillImages(spec)",
      "affected_callers": ["src/llm/generateSpec.ts"],
      "migration_hint": "Remove userPrompt argument from all call sites"
    },
    {
      "what": "MediaTextPropsSchema: imageSrc no longer required",
      "from": "imageSrc: z.string() (required)",
      "to": "imageQuery: z.string() (required), imageSrc: z.string().default('')",
      "affected_callers": ["src/elements/schemas.test.ts"],
      "migration_hint": "Replace imageSrc in test data with imageQuery"
    },
    {
      "what": "ImageBlockPropsSchema: src no longer required",
      "from": "src: z.string() (required)",
      "to": "imageQuery: z.string() (required), src: z.string().default('')",
      "affected_callers": ["src/elements/schemas.test.ts"],
      "migration_hint": "Replace src in test data with imageQuery"
    },
    {
      "what": "GalleryImageSchema: src no longer required",
      "from": "src: z.string() (required)",
      "to": "imageQuery: z.string() (required), src: z.string().default('')",
      "affected_callers": ["src/elements/schemas.test.ts"],
      "migration_hint": "Add imageQuery to gallery image test data"
    }
  ],

  "migrations": [],

  "tests": {
    "unit": {
      "written": [],
      "pending": ["src/elements/schemas.test.ts — update for new schema shapes"],
      "skipped_reason": null
    },
    "integration": { "written": [], "pending": [] },
    "e2e": { "written": [], "pending": [] }
  },

  "important_decisions": [
    "imageQuery is required for modules that always show an image (MediaText, ImageBlock, Gallery items)",
    "imageQuery is optional for modules where image is optional (HeroBanner, CardSchema)",
    "URL fields (imageSrc/src/backgroundImage) default to empty string — populated by imageFiller post-hoc",
    "buildQuery/tokenize/transliterate/extractContext are DELETED, not kept",
    "fillImages userPrompt parameter REMOVED"
  ],

  "notes": [],

  "_meta": {
    "schema_version": "2.0",
    "last_updated": "2026-04-14",
    "last_plan_completed": null
  }
}
